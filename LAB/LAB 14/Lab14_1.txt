#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main(){

 FILE *fp1, *fp2, *fp3;
 char filename[300], c;

 scanf("%s", filename);

 //open the first file
 fp1=fopen(filename, "r");

    if(fp1==NULL){
        printf("Failed to open the file %s\n", filename);
           exit(0);
 }

 //open another file for writing
 fp2=fopen("result.txt", "w");

    if(fp2==NULL){
         printf("Failed to open the file %s\n", filename);
           exit(0);
 }

 //read contents from file

     c=fgetc(fp1);

   while(c!=EOF){
    fputc(c, fp2);
    c=fgetc(fp1);
 }

 //enter the second file to open for reading
  scanf("%s", filename);
  fp1=fopen(filename, "r");

    if(fp1==NULL){
        printf("Failed to open the file %s\n", filename);
           exit(0);
 }

 //open another file for writing

 fp2=fopen("result.txt", "a");

    if(fp2==NULL){
         printf("Failed to open the file %s\n", filename);
           exit(0);
 }

  //read contents from file

     c=fgetc(fp1);

   while(c!=EOF){
    fputc(c, fp2);
    c=fgetc(fp1);
 }
   return 0;
}