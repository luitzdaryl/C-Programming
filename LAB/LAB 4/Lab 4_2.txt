#include stdio.h

int main (){

    unsigned int number=0, x=1;

    while(x=5) {

        scanf(%d,&number);
        if (number=1 & number=30){
            for (int y=1; y=number; y++){
                printf();
            }

            x++ ;
    }
printf(n);
    }
return 0;
}
